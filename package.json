{
  "name": "chirpy",
  "version": "1.0.0",
  "private": true,
  "description": "Annotate, highlight and AI chat with websites",
  "scripts": {
    "build": "bash scripts/build.sh",
    "version": "bash scripts/sync-version.sh && git add manifest.json",
    "release": "bash scripts/release.sh"
  }
}
